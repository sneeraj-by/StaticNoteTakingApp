import React from "react";

function Notes() {
  return (
    <div className="note">
      <h1>Note Taking App</h1>
      <p>
        This is Note Taking App build with JavaScript and React JS. You can add
        more notes, but this is a static version at the moment.
      </p>
    </div>
  );
}

export default Notes;
